<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissões necessárias para as funcionalidades principais -->

    <!-- Microfone: captura de voz para STT -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Internet: inferência via Firebase AI Logic (cloud) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Mantém a CPU acordada durante a bateria de medição (modo teste).
      Sem isto o aparelho entra em Doze com a tela apagada e suspende a coleta no meio —
      observado no Device 1: processo vivo, 0% de CPU, minutos sem avançar.
      É wake lock PARCIAL: a tela fica apagada, senão o consumo do display entraria
      na métrica de energia (H5), que é justamente o que se quer medir do modelo.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Verificação de conectividade para InferenceRouter -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Vibração: feedback háptico opcional -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Feature declarada: microfone é necessário, mas app pode funcionar sem (modo texto) -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:name=".VoiceAssistantApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceAssistant"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.VoiceAssistant.SplashScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
